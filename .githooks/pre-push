#!/usr/bin/env bash
# Mirrors CI lint job: go vet + golangci-lint on the whole module.
# Bypass with: git push --no-verify
set -e

echo "→ go vet ./..."
go vet ./...

echo "→ golangci-lint ./..."
golangci-lint run --timeout=5m ./...
